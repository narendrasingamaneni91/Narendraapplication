# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - run: mvn -B test --file pom.xml  
    - run: mkdir staging && cp target/*.war staging
    - run: pwd
    - run: ls -al    
    - uses: actions/upload-artifact@v1
      with:
        name: Package
        path: staging
    - uses: HurricanKai/Publish-Docker-Github-Action@master
      with:
       name: madhu216/githubactionsrepo
       username: madhu216
       password: madhu216@216
       dockerfile: Dockerfile
    - name: tomcat deploy
      run: curl -v -u admin:admin -T /home/runner/work/madhuapp/madhuapp/target/cangkitsolutions.war 'http://ec2-18-224-15-39.us-east-2.compute.amazonaws.com//manager/text/deploy?path=/cangk_solutions
   # - name: Upload a Build Artifact via S3
   #   uses: ebuckthal/s3-upload-artifact@v7
   #   with:
        # bucket name
   #     bucket: githubaction
   #     # destination key in bucket
   #     key: AKIAYA7GUKHLATBHUZWC
        # file to upload
   #     path: https://githubaction.s3.us-east-2.amazonaws.com/CICD/
